cmake_minimum_required(VERSION 3.13)

project(viking_bio_matter_tests C)

set(CMAKE_C_STANDARD 11)

# Enable CTest
enable_testing()

# Add codec library
add_subdirectory(src/matter_minimal/codec)

# Add tests
add_subdirectory(tests/codec)

message(STATUS "Building TLV codec tests for host platform")
